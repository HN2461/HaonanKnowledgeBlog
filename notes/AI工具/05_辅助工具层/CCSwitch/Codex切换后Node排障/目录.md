---
title: CC Switch 切换 Codex 后 Node 排障目录
date: 2026-04-30
category: AI工具
tags:
  - Codex
  - CC Switch
  - Node.js
  - PowerShell
  - 中转站
  - 故障排查
description: 收纳一组“症状爆在 Codex、根因落在 CC Switch provider 快照与 shell_environment_policy”的 Node 排障资料包，包含主文、采集脚本和对比报告，适合在跨机同步或切换 Codex Provider 后快速定位环境继承异常。
---

# CC Switch 切换 Codex 后 Node 排障目录

> 更新时间：2026-04-30  
> 定位：这是一组可以直接转给别人的 **CC Switch / Codex / Provider 切换 / Node 调用失败** 排障资料。  
> 归类说明：**故障症状出现在 Codex 终端，但真正根因在 CC Switch 的 provider 配置快照、数据库源数据与回写逻辑**，所以专题已迁到 `CCSwitch` 目录维护。

---

## 这组资料适合谁

如果你遇到的是下面这种场景，这组资料基本就是对口的：

1. 外部 PowerShell / CMD 里 `node` 正常
2. 只有 Codex 终端里 `node -e`、`npm`、`vite`、`vitest` 异常
3. 最近刚用 `CC Switch` 切过 Codex 中转或同步过配置
4. 报错里出现 `ncrypto::CSPRNG(nullptr, 0)`、`NTE_PROVIDER_DLL_FAIL`、`0x8009001d`

---

## 当前内容

| 文件 | 用途 |
|------|------|
| [第一篇：CC Switch 切换 Codex Provider 后 Node 调用失败排查修复实录](#/note/AI工具/05_辅助工具层/CCSwitch/Codex切换后Node排障/第一篇_CCSwitch切换CodexProvider后Node调用失败排查修复实录_2026-04) | 主文，完整解释为什么系统终端正常、Codex 终端异常，以及 `inherit = "core"`、`shell_environment_policy` 和 `CC Switch` provider 配置快照的关系 |
| `Node环境一键对比采集脚本.ps1` | 一键采集 Node / npm / PATH / 环境变量 / 加密能力的对比脚本 |
| `对比报告/公司电脑/` | 公司电脑基准样本 |
| `对比报告/个人电脑/` | 个人电脑样本 |

---

## 当前目录结构

```text
public/notes/AI工具/05_辅助工具层/CCSwitch/Codex切换后Node排障/
├─ 目录.md
├─ 第一篇_CCSwitch切换CodexProvider后Node调用失败排查修复实录_2026-04.md
├─ Node环境一键对比采集脚本.ps1
└─ 对比报告/
   ├─ 公司电脑/
   └─ 个人电脑/
```

说明：

1. 这里的主文、脚本、报告是一整组资料，建议一起转发
2. 两份对比报告里如果看到“脚本位置”还是旧路径，那是**报告生成时的原始采样结果**，属于历史记录，不代表当前资料包目录错误

---

## 推荐阅读顺序

1. 先看主文，搞清楚症状、根因、修复链路  
   入口：[第一篇：CC Switch 切换 Codex Provider 后 Node 调用失败排查修复实录](#/note/AI工具/05_辅助工具层/CCSwitch/Codex切换后Node排障/第一篇_CCSwitch切换CodexProvider后Node调用失败排查修复实录_2026-04)
2. 再看 `Node环境一键对比采集脚本.ps1`，知道它会抓哪些维度
3. 最后对照 `对比报告/公司电脑/` 和 `对比报告/个人电脑/`

---

## 转给别人时怎么说最省事

可以直接把下面这段话一起发给对方：

```text
先看 public/notes/AI工具/05_辅助工具层/CCSwitch/Codex切换后Node排障/目录.md 和
public/notes/AI工具/05_辅助工具层/CCSwitch/Codex切换后Node排障/第一篇_CCSwitch切换CodexProvider后Node调用失败排查修复实录_2026-04.md。

重点看：
1. 为什么外部终端正常、Codex 终端却会报 node -e / npm 错误
2. CC Switch 是否把某个 provider 的旧快照重新写回了 .codex/config.toml
3. 当前 .codex/config.toml 里有没有 [shell_environment_policy] 和 inherit = "core"
4. 再对照对比报告里的公司电脑样本和个人电脑样本，定位环境差异
```

---

## 以后自己排查时先看什么

每次在 `CC Switch` 里切完 Codex 中转后，优先做这两步：

```powershell
Select-String -Path C:\Users\HN246\.codex\config.toml -Pattern 'shell_environment_policy|inherit = "core"|inherit = "all"'
pwsh -File scripts/checkNodeRuntime.ps1
```

如果看到：

```toml
[shell_environment_policy]
inherit = "core"
```

而且 `node -e` 又开始失败，就优先回看主文里的修复步骤，不要先去卸载重装 Node。
